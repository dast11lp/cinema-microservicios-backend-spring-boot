spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cinema_auth
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false

server:
  port: 8081